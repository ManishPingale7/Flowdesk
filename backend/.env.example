# Backend Environment Variables

# Django Secret Key - Generate a new one for production!
# Generate: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-yyjy^edj+6l72yevj1rqug=f+kl7@m8n*86jdqndu#3hq6fr(6

# Debug Mode - MUST be False in production
DEBUG=True

# Allowed Hosts - Comma-separated list of domains
# For local: localhost,127.0.0.1
# For production: yourdomain.com,yourapp.railway.app
ALLOWED_HOSTS=localhost,127.0.0.1

# Frontend URL(s) - Comma-separated list for CORS
# For local: http://localhost:5173,http://localhost:3000
# For production: https://yourapp.vercel.app,https://www.yourapp.com
FRONTEND_URL=http://localhost:5173,http://localhost:3000

# Database URL (optional - uses SQLite by default)
# For PostgreSQL in production:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Allow all CORS origins (NOT recommended for production)
# Only use this temporarily for testing
CORS_ALLOW_ALL=False
